// SalesTeamLeaderMyTeam — thin wrapper around the routed MyTeamLayout.
// The actual layout (tabs + Outlet) lives in myTeam/MyTeamLayout.jsx, which is
// what the routes file references. This file is kept so the Packet 6 spec
// filename pattern (Sales*Leader*.jsx) stays consistent across the workspace.
export { default } from "./myTeam/MyTeamLayout";
